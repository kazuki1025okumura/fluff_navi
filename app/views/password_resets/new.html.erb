<div class="w-full h-screen font-sans bg-cover bg-landscape">
  <div class="container flex items-center justify-center flex-1 h-full mx-auto">
    <div class="w-full max-w-lg">
      <div class="leading-loose">
        <%= form_with url: password_resets_path, class: 'max-w-sm p-10 m-auto bg-white bg-opacity-25 rounded shadow-xl', local: true do |f| %>
          <p class="mb-8 text-3xl font-light text-center text-black">
            <%= t('.title') %>
          </p>
          <div class="mb-4 flex-1">
            <%= f.email_field :email, placeholder: 'ご登録いただいたメールアドレス', class: 'input input-bordered input-info w-full max-w-xs' %>
          </div>
          <div class="mb-4 flex-1">
            <p class="mb-8 text-xl">
              パスワードをリセットするためのメールをお送りします。
            </p>
            <p class="text-red-700">
              ※ご登録いただいたメールアドレスではない場合、メールは送信されません。
            </p>
          </div>
          <div class="actions flex items-center justify-between mt-4">
            <%= f.submit t('password_resets.new.submit'), class: 'cursor-pointer py-2 px-4  bg-indigo-600 hover:bg-indigo-700 focus:ring-indigo-500 focus:ring-offset-indigo-200 text-white w-full transition ease-in duration-200 text-center text-base font-semibold shadow-md focus:outline-none focus:ring-2 focus:ring-offset-2  rounded-lg' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
